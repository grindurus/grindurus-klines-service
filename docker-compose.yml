# docker-compose.yml
# $ docker compose up -d --build

version: "3.9"

name: klines-service

services:
  db:
    image: timescale/timescaledb:latest-pg16 # Note: check if pg18 is available, usually 16 is current stable
    container_name: klines-db
    environment:
      POSTGRES_DB: app
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    volumes:
      - pgdata:/home/postgres/data
    networks:
      - grindurus
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres -d app"]
      interval: 3s
      timeout: 5s
      retries: 15

  api:
    build: .
    container_name: klines-api
    command: uvicorn main:app --host 0.0.0.0 --port 8000 --reload
    environment:
      - DATABASE_URL=postgresql://postgres:postgres@db:5432/app
    labels:
      - traefik.enable=true
      - traefik.docker.network=grindurus
      - traefik.http.routers.api.rule=HostRegexp(`klines.*`)
      - traefik.http.routers.api.entrypoints=websecure
      - traefik.http.routers.api.tls.certresolver=le
      - traefik.http.services.api.loadbalancer.server.port=8000
    networks:
      - grindurus
    depends_on:
      db:
        condition: service_healthy

volumes:
  pgdata:

networks:
  grindurus:
    external: true